import RegisterForm from "../components/RegisterForm";

const RegisterPage = () => {
  return (
    <div className="flex items-center justify-center max-h-screen max-w-screen mt-40">
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
